require 'spec_helper'

describe Discourse::Sso::Rails do
  it 'has a version number' do
    expect(Discourse::Sso::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
